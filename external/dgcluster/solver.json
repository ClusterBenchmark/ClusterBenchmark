{
  "name": "dgcluster",
  "class": "learning",
  "reference": "Bhowmick, Meneni, Derr, et al. DGCluster: A Neural Framework for Attributed Graph Clustering via Modularity Maximization. AAAI 2024.",
  "upstream": {
    "repo": "https://github.com/pyrobits/DGCluster.git",
    "commit": "e5ede94ef796a176a092bad14cc20a5e551333f6",
    "note": "Clone + separate driver, no patch: the GNN encoder and modularity loss_fn (main.py) are imported unchanged and main.py/utils.py are left pristine. run_dgcluster.py sets the module globals loss_fn reads and runs training. We run DGCluster unsupervised by overriding its label-based auxiliary objective to zero (it is semi-supervised by default); the override lives in the driver, not the authors' files.",
    "unsupervised": "DGCluster's aux objective uses ground-truth labels; disabled so the comparison stays structural."
  },
  "cwd": "DGCluster",
  "venv": "DGCluster/.venv",
  "entry": "python3 run_dgcluster.py",
  "capabilities": {
    "features": true,
    "clusters": false,
    "threads": true,
    "gpu": true,
    "multi_run": true
  },
  "io": {
    "graph": "--graph",
    "output": "--output-prefix",
    "runs": "--runs",
    "timeout": "--time",
    "threads": "--threads",
    "seed": "--seed",
    "device": "--device",
    "features": "--features"
  },
  "params": {
    "base_model": {
      "flag": "--base-model",
      "default": "gcn",
      "type": "categorical",
      "choices": ["gcn", "gat", "gin", "sage"],
      "doc": "Message-passing backbone. The paper devotes Table 4 to sweeping this (GAT best avg Q, SAGE best avg F1), so it is tunable."
    },
    "feature_kind": {
      "flag": "--feature-kind",
      "default": "ldp+rni",
      "type": "categorical",
      "choices": ["ones", "ldp", "rni", "ldp+rni"],
      "doc": "Synthetic node representation used only when the instance has no real features."
    },
    "learning_rate": {
      "flag": "--learning-rate",
      "default": 0.001,
      "type": "float",
      "tunable": false,
      "doc": "Adam learning rate. Fixed at 1e-3 in the paper (§Other Settings); never swept, so set-only."
    },
    "threshold": {
      "flag": "--threshold",
      "default": 0.5,
      "type": "float",
      "range": [0.1, 1.2],
      "scale": "linear",
      "doc": "Birch distance threshold; the granularity knob (lower yields more clusters). Tunable: the paper controls granularity via lambda (Fig 2), which we disable for the unsupervised setting, so this is our granularity lever for a k-free method. Default 0.5 matches the authors' code."
    }
  }
}
