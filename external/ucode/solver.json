{
  "name": "ucode",
  "class": "learning",
  "reference": "Kalantzi, Karypis (AU-DIS UCODE). Unsupervised Community Detection with modularity-based GNNs.",
  "upstream": {
    "repo": "https://github.com/AU-DIS/UCODE.git",
    "commit": "09f968b2828c684659c420ff60908af46cba20ce",
    "patch": "ucode.patch",
    "note": "The GCN (UCODEncoder.py), loss_modularity_trace (Lossfunction.py), and utils.convert_torch_npz are imported unchanged. The patch is utils.py only: networkx to_scipy_sparse_array compat, a vectorised get_B (the original O(n^2) Python double loop is unusable at scale), and div-by-zero guards. main.py is left pristine and unused; training is run_ucode.py on the shared harness.",
    "fixed": "The number of communities is the encoder output width (hid_units), which the previous integration hardcoded at 16 while ignoring the requested count; it is now set from --clusters."
  },
  "cwd": "UCODE",
  "venv": "UCODE/.venv",
  "entry": "python3 run_ucode.py",
  "cluster_policy": "overprovision",
  "capabilities": {
    "features": true,
    "clusters": true,
    "threads": true,
    "gpu": true,
    "multi_run": true
  },
  "io": {
    "graph": "--graph",
    "output": "--output-prefix",
    "runs": "--runs",
    "timeout": "--time",
    "threads": "--threads",
    "clusters": "--clusters",
    "seed": "--seed",
    "device": "--device",
    "features": "--features"
  },
  "params": {
    "hid_dimension": {
      "flag": "--hid-dimension",
      "default": "256",
      "type": "categorical",
      "choices": [
        "128",
        "256",
        "512"
      ],
      "doc": "Hidden width of the GCN encoder. Default 256 (paper's non-overlapping value, \u00a75.1/\u00a75.4). The hidden dimension is the paper's one sensitivity sweep (Fig 4/6, {8..512}), so it is tunable; choices span the useful plateau."
    },
    "feature_kind": {
      "flag": "--feature-kind",
      "default": "ldp+rni",
      "type": "categorical",
      "choices": [
        "ones",
        "ldp",
        "rni",
        "ldp+rni"
      ],
      "doc": "Synthetic node representation used only when the instance has no real features."
    },
    "learning_rate": {
      "flag": "--learning-rate",
      "default": 0.001,
      "type": "float",
      "tunable": false,
      "doc": "Adam learning rate. Fixed at 1e-3 in the paper (\u00a75.1, 'no hyperparameter tuning'); set-only."
    },
    "weight_decay": {
      "flag": "--weight-decay",
      "default": 0.1,
      "type": "float",
      "tunable": false,
      "doc": "Adam weight decay. Paper uses 0.1 (non-overlapping, \u00a75.1); 0.01 for overlapping. Never swept, so set-only."
    }
  }
}
